# configura_ip_fixo_netplan
Este repositório contém um script Bash para automatizar a configuração de IP estático no **Debian 12** utilizando o **Netplan**, substituindo o método tradicional via `interfaces`.
